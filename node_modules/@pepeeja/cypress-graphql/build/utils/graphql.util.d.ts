import { GraphQLSchema } from 'graphql';
import { MockOptions } from '../types';
export declare class GraphQLMockUtil {
    static mock(operation: string, response: unknown, options: MockOptions, schema: GraphQLSchema | null): void;
    static schemaFromString(schemaString: string): GraphQLSchema;
    static clientSchemaFromString(schemaString: string): GraphQLSchema;
    static schemaStringFromUrl(url: string): Promise<string>;
}
