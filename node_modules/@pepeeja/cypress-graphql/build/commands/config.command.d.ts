import { GraphQLConfig } from '../types';
import { AbstractCommand } from './abstract.command';
export declare class GraphQLConfigCommand extends AbstractCommand {
    command(config: Partial<GraphQLConfig>): Promise<void> | undefined;
}
