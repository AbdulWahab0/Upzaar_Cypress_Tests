import { MockOptions } from '../types';
import { AbstractCommand } from './abstract.command';
export declare class GraphQLMockCommand extends AbstractCommand {
    command(operation: string, response: unknown, mockOptions?: MockOptions): void;
}
