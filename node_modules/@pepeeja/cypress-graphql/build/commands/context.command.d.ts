import { GraphQLSchema } from 'graphql';
import { MockOptions } from '../types';
import { AbstractCommand } from './abstract.command';
declare type GraphQLMockContextProvider = (gql: GraphQLMockContext) => void;
export declare class GraphQLContextCommand extends AbstractCommand {
    command(gql: GraphQLMockContextProvider): void;
}
declare class GraphQLMockContext {
    private _schema;
    private _options;
    mock(operation: string, response: unknown): this;
    schemaFromString(schemaString: string): this;
    schema(schema: GraphQLSchema): this;
    options(options: MockOptions): this;
}
export {};
